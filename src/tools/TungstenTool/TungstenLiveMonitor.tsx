import React from 'react'

// Stub for external builds — TungstenLiveMonitor is ant-only and never rendered
// in external builds (guarded by `"external" === 'ant'` in REPL.tsx).
export function TungstenLiveMonitor(): React.ReactNode {
  return null
}
